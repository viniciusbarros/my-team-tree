<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8"/>
        <title>Slim 3 - Base project</title>
        <link href='//fonts.googleapis.com/css?family=Lato:300' rel='stylesheet' type='text/css'>
        <style>
            body {
                margin: 50px 0 0 0;
                padding: 0;
                width: 100%;
                font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
                text-align: center;
                color: #aaa;
                font-size: 18px;
            }

            h1 {
                color: #719e40;
                letter-spacing: -3px;
                font-family: 'Lato', sans-serif;
                font-size: 100px;
                font-weight: 200;
                margin-bottom: 0;
            }
        </style>
    </head>
    <body>
        {% if name is not empty %}
            Welcome {{ name }} 
        {%endif%}
        <h1>Package based on Slim 3 </h1>
        <div>with a few extras</div>
        <h2>Extras</h2>

        {%for extra in extras %}
            - {{ extra }} <br>
        {%endfor%}

    </body>
</html>
